import React from 'react';
import { TopMenuBar } from './components/TopMenuBar/TopMenuBar';

const Page: React.FC = () => {
    return (
        <div>
            <TopMenuBar />
        </div>
    );
};

export default Page;